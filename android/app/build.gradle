plugins {
    id("com.android.application")
    id("kotlin-android")
    id("dev.flutter.flutter-gradle-plugin") // Flutter Gradle Plugin must be applied after Android & Kotlin
}

android {
    namespace = "com.pawfect.app"  // Ensure this matches your package name
    compileSdk = 35  // Use latest SDK version

    defaultConfig {
        applicationId = "com.pawfect.app"  // Unique application ID
        minSdk = 21  // Minimum SDK version
        targetSdk = 35  // Target SDK version
        versionCode = 1  // Update when releasing new versions
        versionName = "1.0"
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    buildTypes {
        release {
            minifyEnabled = false
            shrinkResources = false
            // Add signing configuration here when releasing the app
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation("com.google.android.gms:play-services-maps:18.2.0") // Google Maps SDK
}
